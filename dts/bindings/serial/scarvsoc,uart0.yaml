title: SCAR-V SoC UART

description: |
    This binding gives a base representation of the SCAR-V SoC UART

compatible: "scarvsoc,uart0"

include: uart-controller.yaml

properties:
    reg:
      required: true

    interrupts:
      required: false
